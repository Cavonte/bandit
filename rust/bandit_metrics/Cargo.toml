[package]
name = "bandit_metrics"
version = "0.1.0"
edition = "2021"

[lib]
name = "bandit_metrics"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }
